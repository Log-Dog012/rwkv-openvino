- 本地环境：openvino 2026.3.0 预编译在 C:/Users/Mcsof/Application/openvino_2026.3.0，用 setupvars.bat 设环境 + PYTHONPATH=python 即可 import（AI 环境 Python 3.12 与 cp312 pyd 匹配）。conda AI 环境 Python 3.12.13，torch 2.13.0+xpu，numpy 2.5.1，gguf 用 uv pip 装。不再用沙箱的 fastgit 代理/8GB cgroup 限制——本地 32GB 内存充裕。
- 本机 llama.cpp 已编译三后端在 C:/Users/Mcsof/Application/llama.cpp/：llama-vulkan/（Intel Arc 核显，uma=1）、llama-sycl/、llama-openvino/，每后端含 llama-cli.exe + llama-bench.exe。master 源码在 master/ 但未编译。llamacpp openvino 后端对 RWKV 报 CPY 错：根因是 ov 后端按静态 Transformer 写，cache_r_l0 这个 RNN 持久状态 view 被分到 OPENVINO0 buffer，跨 buffer 的 CPY（ov↔CPU）它不接，supports_op 走逆向 return true 逻辑后 break 崩。本仓库自建 OV IR 链路绕开它。
- bash 工具上限 300s，detached/cmd 独立窗口在本机系统性崩被吞（stdout 缓冲被 OS 回收，崩前刷不出来，progress flag 也写不全）。绕开法：①前台分段跑用接力脚本（gen接力.py phase=sweep 存 npz / phase=gen 加载续跑）②编译缓存 ov.properties.cache_dir 启用后二次编译 8×加速。性能模式 hint.performance_mode 这个 OV 版 CPU 插件不支持，set_property 要用 ov.properties.inference_num_threads enum 键不是裸字符串。
- gguf_kquant_repack.py 已向量化：repack_q4_k 从逐块 Python 循环 2.27s → 0.038s（60×），_repack_q6_k 从 19.1s → 2.2s（9×），bit-exact 对齐 gguf ground truth。让 13.3b 构图从预估 18min 压到 274s。13.3b 规格 V=65536 C=4096 L=61 H=64 N=64（与 7.2b 的 V=4096 C=65536 反过来，不同 GGUF maker emb 存法不同但脚本 _dequant_np 取物理 shape 自动适配，无 bug）。
